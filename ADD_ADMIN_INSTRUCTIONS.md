# Создание администратора GROB RAT

## 🛠️ Способ 1: Использование Node.js скрипта (РЕКОМЕНДУЕМЫЙ)

### Шаг 1: Откройте терминал в папке проекта

```bash
cd c:\Users\m1tka1337\Desktop\GROB RAT
```

### Шаг 2: Запустите скрипт добавления администратора

```bash
node add-admin.js test kmrat
```

Где:
- `test` - желаемое имя администратора
- `kmrat` - пароль администратора

### Результат:

```
✅ Администратор успешно создан!

Данные входа:
  Логин:      test
  Пароль:     kmrat
  ID:         [автоматически сгенерированный ID]
  UID:        [автоматически сгенерированный UID]
```

---

## 🌐 Способ 2: Использование API endpoint

### Шаг 1: Убедитесь, что приложение запущено

```bash
npm run dev
```

### Шаг 2: Отправьте POST запрос на `/api/auth/create-admin`

**cURL:**
```bash
curl -X POST http://localhost:3000/api/auth/create-admin \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"kmrat"}'
```

**PowerShell:**
```powershell
$body = @{
    username = "test"
    password = "kmrat"
} | ConvertTo-Json

Invoke-WebRequest -Uri "http://localhost:3000/api/auth/create-admin" `
  -Method POST `
  -ContentType "application/json" `
  -Body $body
```

### Ответ:
```json
{
  "user": {
    "id": "...",
    "username": "test",
    "is_admin": true,
    "uid": "..."
  },
  "message": "Администратор успешно создан!"
}
```

---

## ✅ Вход в систему

После создания администратора вы сможете войти с помощью:

- **Логин:** `test`
- **Пароль:** `kmrat`

---

## 📝 Примеры других командных строк

```bash
# Администратор с другим именем и паролем
node add-admin.js admin123 secretpass123

# Администратор с простыми данными
node add-admin.js root 1234

# Администратор на русском (если поддерживается)
node add-admin.js администратор паролькроткий
```

---

## ⚠️ Важно

1. **Длина логина:** минимум 3 символа
2. **Длина пароля:** минимум 4 символа
3. **Уникальность:** Не может быть администратора с одинаковым старым именем
4. **База данных:** Скрипт автоматически создаст файл `grob.db` если его нет
5. **Default Admin:** По умолчанию существует администратор с логином `ORIXMAN` и паролем `180886` (не может быть удален)

---

## 🔧 Если что-то не работает

1. Проверьте, установлены ли зависимости: `npm install`
2. Убедитесь, что у вас есть права на запись в папку проекта
3. Проверьте наличие файла `better-sqlite3` в node_modules
4. Попробуйте удалить `grob.db` и запустить скрипт заново
